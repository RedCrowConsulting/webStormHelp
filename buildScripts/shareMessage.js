const chalk = require('chalk');

/* eslint-disable no-console */

console.log(chalk.green('Starting app in dev mode...'));
console.log(chalk.green('Requesting local tunnel...'));
console.log(chalk.green(`NOTE: Retry on connection refusals` +
  ` (server overload is common)`));
